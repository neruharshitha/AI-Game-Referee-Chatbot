# AI-Game-Referee-Chatbot
A 3-round Rock-Paper-Scissors-Plus game judge built in Python. Uses Gemini model for bot moves while enforcing rules via user-defined tools (validate, resolve, update state). Game state stays in-memory using Pydantic. No DB, UI, or servers.
